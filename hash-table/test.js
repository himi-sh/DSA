// function myFunc(prev, current) {

//       return prev - current;
//  }


// const numbers = [1, 2, 0];
// numbers.reduce(myFunc);


 const numbers = [1, 2, 3, 4];
 const plus = numbers.map(n => n + 2);
 console.log(plus);

numbers.forEach(n=> console.log(n));
